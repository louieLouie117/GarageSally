<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <script>
        $(document).ready(function () {


            $('#SearchFormHandler').submit(function (event) {
                event.preventDefault();
                if (!navigator.geolocation) {
                    alert(`Your browser doesn't support Geolocation`);
                }
                document.querySelector("#IndexSearchResults").style.display = "grid";

                let City = document.querySelector("#HomeSearch").value
                let Zipcode = document.querySelector("#HomeSearch").value

                var jsonData = {
                    city: City,
                    zipcode: Zipcode,
                }

                console.log(jsonData);

                $.ajax({
                    type: "GET",
                    url: '@Url.Action("HomeSearchHandler", "Home")',
                    data: jsonData,
                    success: SuccessStatus,
                    error: ErrorStatus,
                    complete: CompleteStatus
                })

            })
            const SuccessStatus = (result) => {
                console.log("You have sucessfuly searched...");
                LoadSearch();
            };
            const ErrorStatus = (xhr, status, strErr) => {
                console.log("There was an error when searching!");
            };
            const CompleteStatus = (xhr, status) => {
                console.log("Search has completed...");
            };


        })

        const LoadSearch = () => {
            $('#HomeSearchResults').get("/HomeSearchHandler #HomeSearchResults", data => {
                data = JSON.parse(data);
                RenderSearch(data);
                console.log("search data", data)
            });
        };
        @* $.get("/HomeSearchHandler", data => {
                RenderSearch(data);
                }); *@

        const RenderSearch = (data) => {
            $.each(data.data, function (i, v) {
                console.log("Value", v)
                const TimeFormat = {
                    hour12: true,
                    hour: "numeric",
                    minute: "numeric", seconds: "numeric"
                }
                const DateFormat = {
                    weekday: "long",
                    month: "long",
                    day: "numeric"
                };
                const CheckDateFormat = {
                    year: "numeric",
                    month: "long",
                    day: "numeric"
                };
                let dateFormated = new Date(v.startDate).toLocaleDateString("en-US", DateFormat);
                let startTimeFormatted = new Date(v.startTime).toLocaleTimeString("en-US", TimeFormat);
                let endTimeFormatted = new Date(v.endTime).toLocaleTimeString("en-US", TimeFormat);
                let checkDate = new Date(v.startDate);
                let checkToday = new Date();
                checkToday.setDate(checkToday.getDate() - 1);

                let garageItemId = v.garageSaleId;

                if (checkDate < checkToday) {
                    console.log("this event has expired")
                } else {
                    $(`<li id="${garageItemId}">
                        <section>
                            <header>
                                <h1>${dateFormated}</h1>
                                <h2>${startTimeFormatted}-${endTimeFormatted}</h2>
                            </header>
                              <footer>
                                <h3> ${v.city} ${v.state}</h3>
                                <p>${v.description}</p>
                            </footer>
                            <main>
                                <a href="http://maps.google.com/?q=${v.streetNumber} ${v.streetName}, ${v.state}, ${v.zipcode}" target="blank"><button>Directions</button></a>
                                @* <p>20 people visted.</p> *@
                            </main>
                        </section>
                    </li>`).prependTo("#HomeSearchResults");
                }
            });
        };


    </script>
</head>

<body>

    <div class="search-container" id="searchPartial">

        <header>
            <form id="SearchFormHandler">
                <input type="text" name="" id="HomeSearch" placeholder="City ST or Zip Code">
                <button>Search</button>
            </form>


        </header>

        <main id="IndexSearchResults">
            <h3>Search resutls</h3>
            <ul id="HomeSearchResults">
                <li>Tes</li>
                <li>Test</li>
                <li>Test</li>
                <li>Test</li>
                <li>Test</li>
                <li>Test</li>
                <li>Test</li>
                <li>Test</li>
                <li>Test</li>
                <li>Test</li>

            </ul>

        </main>

    </div>



</body>

</html>